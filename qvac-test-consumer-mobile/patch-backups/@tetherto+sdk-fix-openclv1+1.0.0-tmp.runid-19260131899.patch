diff --git a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/llamacpp/create-model.js b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/llamacpp/create-model.js
index 96070de..0d0e99e 100644
--- a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/llamacpp/create-model.js
+++ b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/llamacpp/create-model.js
@@ -1,6 +1,21 @@
-import LlmLlamacpp from "@tetherto/llm-llamacpp";
-import EmbedLlamacpp from "@qvac/embed-llamacpp";
 import { Readable } from "bare-stream";
+
+let LlmLlamacpp = null;
+let EmbedLlamacpp = null;
+async function getLlmLlamacpp() {
+    if (!LlmLlamacpp) {
+        const mod = await import("@tetherto/llm-llamacpp");
+        LlmLlamacpp = mod.default;
+    }
+    return LlmLlamacpp;
+}
+async function getEmbedLlamacpp() {
+    if (!EmbedLlamacpp) {
+        const mod = await import("@qvac/embed-llamacpp");
+        EmbedLlamacpp = mod.default;
+    }
+    return EmbedLlamacpp;
+}
 import { createStreamLogger } from "../../../../logging/index.js";
 import { parseModelPath } from "../../../utils/index.js";
 // Mimic qvac-lib-dl-filesystem functionality
@@ -32,7 +47,8 @@ function transformLlmConfig(llmConfig) {
     }
     return transformed;
 }
-export function createLlmModel(modelId, modelPath, llmConfig, projectionModelPath) {
+export async function createLlmModel(modelId, modelPath, llmConfig, projectionModelPath) {
+    const LlmClass = await getLlmLlamacpp();
     const { dirPath, basePath } = parseModelPath(modelPath);
     const dl = createFilesystemDLMimic();
     const logger = createStreamLogger(modelId, "llamacpp:llm");
@@ -49,9 +65,10 @@ export function createLlmModel(modelId, modelPath, llmConfig, projectionModelPat
         modelPath,
         modelConfig: llmConfigStrings,
     };
-    return new LlmLlamacpp(args, llmConfigStrings);
+    return new LlmClass(args, llmConfigStrings);
 }
-export function createEmbeddingsModel(modelId, modelPath, embedConfig) {
+export async function createEmbeddingsModel(modelId, modelPath, embedConfig) {
+    const EmbedClass = await getEmbedLlamacpp();
     const { dirPath, basePath } = parseModelPath(modelPath);
     const dl = createFilesystemDLMimic();
     const logger = createStreamLogger(modelId, "llamacpp:embed");
@@ -63,5 +80,5 @@ export function createEmbeddingsModel(modelId, modelPath, embedConfig) {
         modelName: basePath,
         modelPath,
     };
-    return new EmbedLlamacpp(args, embedConfig.config);
+    return new EmbedClass(args, embedConfig.config);
 }
diff --git a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/load-model.js b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/load-model.js
index 3e82688..c0c73e6 100644
--- a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/load-model.js
+++ b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/load-model.js
@@ -34,22 +34,22 @@ export async function loadModel(params) {
     let model;
     switch (modelType) {
         case "llm":
-            model = createLlmModel(modelId, modelPath, modelConfig, projectionModelPath);
+            model = await createLlmModel(modelId, modelPath, modelConfig, projectionModelPath);
             break;
         case "whisper":
-            model = createWhisperModel(modelId, modelPath, modelConfig, vadModelPath);
+            model = await createWhisperModel(modelId, modelPath, modelConfig, vadModelPath);
             break;
         case "embeddings":
-            model = createEmbeddingsModel(modelId, modelPath, modelConfig);
+            model = await createEmbeddingsModel(modelId, modelPath, modelConfig);
             break;
         case "nmt":
-            model = createNmtModel(modelId, modelPath, modelConfig);
+            model = await createNmtModel(modelId, modelPath, modelConfig);
             break;
         case "tts":
             if (!eSpeakDataPath) {
                 throw new Error("eSpeakDataPath is required for TTS models");
             }
-            model = createTtsModel(modelId, modelPath, modelConfig, ttsConfigModelPath, eSpeakDataPath);
+            model = await createTtsModel(modelId, modelPath, modelConfig, ttsConfigModelPath, eSpeakDataPath);
             break;
         default:
             // Should never happen
diff --git a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/translation/create-model.js b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/translation/create-model.js
index 3ee9e52..08831d2 100644
--- a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/translation/create-model.js
+++ b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/translation/create-model.js
@@ -1,9 +1,18 @@
-import TranslationNmtcpp from "@qvac/translation-nmtcpp";
 import { Readable } from "bare-stream";
 import {} from "../../registry/model-registry.js";
 import { INDICTRANS_LANGUAGES } from "../../../../schemas/index.js";
 import { createStreamLogger } from "../../../../logging/index.js";
 import { parseModelPath } from "../../../utils/index.js";
+
+let TranslationNmtcpp = null;
+async function getTranslationNmtcpp() {
+    if (!TranslationNmtcpp) {
+        const mod = await import("@qvac/translation-nmtcpp");
+        TranslationNmtcpp = mod.default;
+    }
+    return TranslationNmtcpp;
+}
+
 // Mimic qvac-lib-dl-filesystem functionality
 function createFilesystemDLMimic(options) {
     return {
@@ -18,7 +27,8 @@ function createFilesystemDLMimic(options) {
         stop: () => Promise.resolve(),
     };
 }
-export function createNmtModel(modelId, modelPath, nmtConfig) {
+export async function createNmtModel(modelId, modelPath, nmtConfig) {
+    const NmtClass = await getTranslationNmtcpp();
     const { dirPath, basePath } = parseModelPath(modelPath);
     const dl = createFilesystemDLMimic({
         dirPath: "./",
@@ -38,8 +48,8 @@ export function createNmtModel(modelId, modelPath, nmtConfig) {
     const config = {
         modelType: INDICTRANS_LANGUAGES.includes(nmtConfig.from) &&
             INDICTRANS_LANGUAGES.includes(nmtConfig.to)
-            ? TranslationNmtcpp.ModelTypes.IndicTrans
-            : TranslationNmtcpp.ModelTypes.Opus,
+            ? NmtClass.ModelTypes.IndicTrans
+            : NmtClass.ModelTypes.Opus,
     };
-    return new TranslationNmtcpp(args, config);
+    return new NmtClass(args, config);
 }
diff --git a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/tts/create-model.js b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/tts/create-model.js
index 777bfc1..f752234 100644
--- a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/tts/create-model.js
+++ b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/tts/create-model.js
@@ -1,5 +1,13 @@
-import ONNXTTS from "@qvac/tts-onnx";
 import {} from "../../registry/model-registry.js";
+
+let ONNXTTS = null;
+async function getONNXTTS() {
+    if (!ONNXTTS) {
+        const mod = await import("@qvac/tts-onnx");
+        ONNXTTS = mod.default;
+    }
+    return ONNXTTS;
+}
 import {} from "../../../../schemas/index.js";
 import { createStreamLogger } from "../../../../logging/index.js";
 import { parseModelPath } from "../../../utils/index.js";
@@ -18,7 +26,8 @@ function createFilesystemDLMimic(options) {
         ready: () => Promise.resolve(),
     };
 }
-export function createTtsModel(modelId, modelPath, ttsConfig, ttsConfigModelPath, eSpeakDataPath) {
+export async function createTtsModel(modelId, modelPath, ttsConfig, ttsConfigModelPath, eSpeakDataPath) {
+    const TtsClass = await getONNXTTS();
     const { dirPath, basePath: fileName } = parseModelPath(modelPath);
     const dl = createFilesystemDLMimic({
         dirPath: "./",
@@ -36,5 +45,5 @@ export function createTtsModel(modelId, modelPath, ttsConfig, ttsConfigModelPath
     const config = {
         language: ttsConfig.language,
     };
-    return new ONNXTTS(args, config);
+    return new TtsClass(args, config);
 }
diff --git a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/whispercpp/create-model.js b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/whispercpp/create-model.js
index 325552a..178c53b 100644
--- a/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/whispercpp/create-model.js
+++ b/node_modules/@tetherto/sdk-fix-openclv1/dist/server/bare/addons/whispercpp/create-model.js
@@ -1,5 +1,13 @@
-import TranscriptionWhispercpp from "@qvac/transcription-whispercpp";
 import { Readable } from "bare-stream";
+
+let TranscriptionWhispercpp = null;
+async function getTranscriptionWhispercpp() {
+    if (!TranscriptionWhispercpp) {
+        const mod = await import("@qvac/transcription-whispercpp");
+        TranscriptionWhispercpp = mod.default;
+    }
+    return TranscriptionWhispercpp;
+}
 import {} from "../../registry/model-registry.js";
 import {} from "../../../../schemas/index.js";
 import { createStreamLogger } from "../../../../logging/index.js";
@@ -21,7 +29,8 @@ function createFilesystemDLMimic(options) {
         downloadFiles: () => Promise.resolve([]),
     };
 }
-export function createWhisperModel(modelId, modelPath, whisperConfig, vadModelPath) {
+export async function createWhisperModel(modelId, modelPath, whisperConfig, vadModelPath) {
+    const WhisperClass = await getTranscriptionWhispercpp();
     const { dirPath, basePath } = parseModelPath(modelPath);
     // Handle VAD model path
     let vadModelName = "";
@@ -52,5 +61,5 @@ export function createWhisperModel(modelId, modelPath, whisperConfig, vadModelPa
         ...(contextParams && { contextParams }),
         ...(miscConfig && { miscConfig }),
     };
-    return new TranscriptionWhispercpp(args, config);
+    return new WhisperClass(args, config);
 }
