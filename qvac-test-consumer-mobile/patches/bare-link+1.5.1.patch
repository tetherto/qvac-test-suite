diff --git a/node_modules/bare-link/lib/platform/android.js b/node_modules/bare-link/lib/platform/android.js
index a980698..a7f9f66 100644
--- a/node_modules/bare-link/lib/platform/android.js
+++ b/node_modules/bare-link/lib/platform/android.js
@@ -62,6 +62,11 @@ module.exports = async function android(base, pkg, name, version, opts = {}) {
       if (err.code !== 'ENOENT') throw err
     }
 
-    await run(patchelf, [...replacements, so], opts)
+    // Skip patchelf on Windows when building for Android (patchelf.exe can't patch Linux ELF binaries)
+    if (process.env.SKIP_PATCHELF !== '1' && process.env.PATCHELF_SKIP !== '1' && process.env.BARE_RUNTIME_SKIP_PATCHELF !== '1' && process.platform !== 'win32') {
+      await run(patchelf, [...replacements, so], opts)
+    } else if (process.platform === 'win32') {
+      console.log(`[bare-link] Skipping patchelf for ${so} on Windows (Android binaries don't need patching on Windows host)`)
+    }
   }
 }
