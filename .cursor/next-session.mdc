# Next Session Plan

## Current State

**Completed**: Steps 1-8
- Framework working end-to-end
- All 217 SDK tests ported (198 active, 19 skipped)
- JSON/HTML reports + comparison working
- GitHub Actions publish workflow ready

**Branch**: `feature/framework-extraction`

## Options for Next Session

### Option 1: Continue Test Migration (Most Momentum)

Port remaining test categories incrementally:
- Transcription tests (~12 tests)
- Embedding tests (~14 tests)
- RAG tests (~11 tests)
- Translation tests (~3 tests)
- Tools/function calling tests (~20 tests)
- Error handling tests

**Estimated**: 2-3 hours to port all categories
**Benefit**: Complete feature parity with old structure
**Then**: Delete old structure (Step 7)

### Option 2: Reports & Comparison (Step 8)

Implement report generation:
- JSON reports (per-platform)
- HTML reports (existing, adapt)
- report:compare command
- report:format command (JSON → markdown)

**Estimated**: 2-3 hours
**Benefit**: Get regression detection working
**Caveat**: Need more tests to make reports interesting

### Option 3: GitHub Actions (Steps 9-12)

Skip ahead to CI/CD:
- Publish framework to GPR
- Create GitHub Actions workflow
- Test in SDK repo
- PR comment automation

**Estimated**: 3-4 hours
**Benefit**: Get automation working early
**Caveat**: CI is less interesting with only 7 tests

## Recommendation

**Continue porting all tests systematically**:

### Immediate Next Steps

1. **Port completion tests** (~50 tests)
   - Create generic completion handler
   - Handles all parameter variations (temperature, top_p, penalties, etc.)
   - Port all completion test definitions
   
2. **Port transcription tests** (~12 tests)
   - Audio file handling
   - Create transcription executor
   
3. **Port embedding tests** (~14 tests)
   - Text/code embedding
   - Create embedding executor
   
4. **Port RAG tests** (~11 tests)
   - Document chunking
   - Embeddings + retrieval

5. **Port remaining** (translation, tools, cache, errors)

### Testing Strategy

- Use `--filter` flag to test categories incrementally
- Validate each category before moving to next
- Compare pass rates with old structure

### Timeline

- Completion tests: 1-2 hours
- Other categories: 2-3 hours  
- Total porting: 3-5 hours

This maintains momentum and completes feature parity before moving to reports/CI/CD.

## Critical ToDos Before Next Session

- [ ] Review any issues encountered today
- [ ] Check if old structure still works (fallback option)
- [ ] Decide: port more tests OR move to reports/CI

## Notes

- Environment variable baking needed for mobile (documented in future-improvements.mdc)
- Desktop build now defaults to cross-platform (--platform is optional)
- Test executor pattern enhancement documented for later
